- 競争優位に結びつくのは？
    - 中核の業務領域
- 競合他社も同じ解決手段を使う可能性があるのは？
    - 一般的な業務領域
- 変更を頻繁に行うのは？
    - 中核の業務領域
- 同じ言葉の定義に貢献できるのは？
    - プロジェクトの全ての関係者
- 同じ言葉はどこで使う？
    - 全ての意図を伝える場面で使う
- 業務エキスパートと会話する時に使える業務の概念は？
- 
-
-
- 異なる部門に参加している業務エキスパートが共通の言葉を複数の意味で使っていたら？
- 業務領域と区切られた文脈の違いとは？
    - 区切られた文脈は設計・業務領域は発見
- 区切られた文脈は何の境界？
    - モデルの境界、そしてモデルは対応する区切られた文脈の中でのみ意味を持つ。区切られた文脈は所有権の境界でもある
- 区切られた文脈の大きさとは？
    - プロジェクトと状況によって異なる
- 区切られた文脈とチームの所有権について
    - 区切られた文脈を所有できるのは１つのチームだけ、だが１つのチームが複数の文脈を所有することは可能である
- 
- 
- 中核の業務領域に避けるべき連携方法は？
    - 互いに独立
- モデル変換装置を使う可能性の高い下流の業務領域は？
    - 中核の業務領域
- 共用サービスを提供する可能性が高い上流の業務領域は？
    - 中核の業務領域
- ある意味で区切られた文脈の原則違反となる連結方法とは？
    - モデルの共有
- 中核の業務領域に適した方法とは？
    - ここまでに学んだことは何も使えない
- 
- 
- 
- 値オブジェクトは不変（イミュータブル）であり、データとロジックを持つ
- 集約の境界を設計する一般原則は？
    - 業務データの一貫性を保証できる限り集約はできるだけ小さく設計すること
- 集約のインスタンスを単一のトランザクションとしてコミットする理由とは？
    - トランザクションの境界を正しく設定するため
- ドメインモデルを構成する部品についての説明
    - 値オブジェクトはエンティティの属性を記述できる・集約は一つまたは複数のエンティティを含む
- アクティブレコードと集約の違いは？
    - 集約は関連する全ての業務ロジックをカプセル化するが、アクティブレコードは業務ロジックを別のオブジェクトに記述することができる
- 業務イベントと値オブジェクトの関係について説明を
    - 業務イベントは値オブジェクトを使って業務で発生した事実を記述する
- 業務イベントの履歴から状態を投影するやり方について説明を
    - さまざまな切り口の状態を投影できるし、後から新たな切り口の投影を追加することもできる
- 状態変更ありの集約とイベント履歴式の集約の違いについて説明を
    - どちらも業務イベントを生成できるが、業務イベントを「真実を語る唯一の情報源」とするのはイベント履歴式の集約だけ、さらに、イベント履歴式の集約は、全ての状態遷移に対して業務イベントを生成するモノである。
- アクティブレコードで業務ロジックを実装する際に選択できる技術方式は？
    - レイヤードアーキテクチャ・CQRS
- インフラの関心ごとから業務ロジックを分離できる技術方式は？
    - ポートとアダプター・CQRS
- ポートとアダプターを実装する場合で、クラウドベンダーのマネージャー式のメッセージ通信基盤を統合する必要がある場合、これに関する実装を行うのは？
    - インフラストラクチャ層
- CQRSについて説明を
    - 非同期投影はスケールしやすい・コマンドは強力な一貫性を持つモデルに基づいていれば情報を返すことが可能
- CQRSは同じ業務オブジェクトの永続モデルで表現することを可能にし、同じ区切られた文脈内で複数のモデルで作業することを許可する、これはモデルの境界である区切られた文脈の概念と矛盾するかな？
    - CQRSによって投影された複数のモデルを扱うことは、モデルの境界であるという区切られた文脈の要件に矛盾しない。さまざまなモデルの中で、１つのモデルだけが「真実を語る唯一の情報源」となり、集約の状態の変更に使われるから
- 区切られた文脈を繋ぐ方法で、モデルの変換ロジックを組み込む必要があるものといえば？
    - モデル変換装置・共用サービス
- 送信箱の狙いは？
    - メッセージの確実な発行
- 送信箱はメッセージ通信基盤にメッセージを発行する以外にどんな機能の実現方法として使える？
    - 外部のコンポーネントを非同期的に実行できる。例：電子メールの送信など
- サーガとプロセスマネージャーの違いは？
    - プロセスマネージャーは明示的に起動する必要があるが、サーガは関連する業務イベントを検知した時に実行されること・プロセスマネージャーは複雑な業務フローの実現に適している
- 業務ロジックの実装方法と全体を組み立てる技術方式は最初に何を選ぶべき？テスト方針は？
    - イベント履歴式ドメインモデル、CQRS（コマンド・クエリ責任分離）、ピラミッド式（単体テスト中心）のテスト
- 
- 
- 
- 組織が拡大すると区切られた文脈の連携方法はどう変わりそうかな？
    - 良きパートナーから利用者と供給者の関係（従属、モデル変換装置または共用モデル）
- 区切られた文脈同士の連携方法が、従属から互いに独立に変化するとしたら、どういう状況が考えられる？
    - 他のチームと協力することに苦労している・重複している機能は補完的な業務領域か一般的な業務領域の機能である
- 補完的な業務領域が中核の業務領域に変わる兆候とは？
    - 既存のモデルの改善が大変になってきた・変更の頻度が多い
- 新たな事業機会を見つけた時に起きる変化とは？
    - 補完的な業務領域が中核の業務領域に変わる・一般的な業務領域が中核の業務領域に変わる
- 
- イベントストーミングの場に招待すべき人は？
    - 探求したい業務領域の知識を持つ全てのステークホルダー
- イベントストーミングを実施するのに適している時とは？
    - 新たな言葉を作る時・新たな事業領域を探求する時・既存システムの失われた知識を学び直す時・新規チームメンバーを迎え入れる時・業務プロセスを最適化する豊富を発見したい時
- イベントおストーミングの実施によって期待できる成果は？
    - 「事業活動に対するより良い理解の共有・同じ言葉を作り上げるための強固な基盤・事業領域内で考慮されていない空白地帯の発見・ドメインモデルを実施するために利用できるイベント中心のモデル」（しかしこれらはセッションの目的により異なるものである）
- ドメイン駆動設計の考え方とやり方を既存システムのプロジェクトに取り入れる時、最初にやるべきことは？
    - 自社の事業活動と事業方針をを詳しく調べること
- 既存システムを適切な構造に改善するためにストラングラー方式で取り組む場合、ドメイン駆動設計の設計原則と矛盾するのはどのような場合？
    - 複数の区切られた文脈が１つのデータベースを共有する場合・中核の業務領域で、移行時に追加した区切られた文脈の機能と既存システムの機能で業務ロジックが重複する場合
- 大抵アクティブレコードで記述している業務ロジックをイベント履歴式ドメインモデルに直接移行することは良いアイデアではない。これはなぜ？
    - 「一旦状態変更ありのモデルに移行することで、集約の境界のリファクタリングを学習しながら進められるから」・「段階的に移行していく方が安全だから」
- 集約を取り入れる時、チームのメンバーから「できるだけ多くのエンティティを１つの集約にまとめた方が、業務ロジックが一箇所にまとまって処理が追いかけやすいと思うのだが、なんでそうしないの？」と言われた場合どう答える？
    - 集約の境界が区切られた文脈全体に広がると、区切られた文脈のあらゆるデータが巨大なトランザクション管理の対象になるんだ。また、このやり方は性能問題が起きることが明白だよ。すると、結果として、集約がデータの一貫性を保証するという前提が崩れてしまうんだ。
- 区切られた文脈とマイクロサービスとの関係性は？
    - 全てのマイクロサービスは区切られた文脈であるが、全ての区切られた文脈はマイクロサービスではない
- マイクロサービスで「マイクロ（最小）」にすべきものとは？
    - サービスの境界を超えて公開され、その公開インンターフェースに反映される業務知識とその複雑さについての知識
- 無難なコンポーネントの境界といえるものと言えば？
    - 区切られた文脈（最も広い）とマイクロサービス（最も狭い）の間の境界
- マイクロサービスを集約の境界に合わせることは良い設計かな？
    - 事業活動を展開する領域によって判断が分かれるので一概にはいえない
- **イベント駆動式アーキテクチャでは、コンポーネントの境界を超えて情報をやり取りするためのイベントを定義する・イベントソーシングでは、区切られた文脈の内部でしか使わないイベントを定義する**
- 状態の変更内容を伝える手段として、最も適切なイベントのカテゴリーは？
    - イベントによる状態転送
- 区切られた文脈を繋ぐための公開イベントを明示的に設計する連携方法とは？
    - 共用サービス
- サービス１とサービス２を非同期で繋いでいることを考えます。サービス１はデータを外部に伝える必要があり、サービス２はサービス１の最新の状態を知る必要があります。この場合、どのイベントカテゴリーを使うのが適切かな？
    - サービス１がイベント通知メッセージを発行し、メッセージを受け取ったサービス２がサービス１に最新状態を問い合わせる。
- 業務系モデルと分析系モデルの違いを説明してください
    - 分析系モデルは御油無形モデルよりも多様な問い合わせに対応する必要がある・業務系データは日々の業務活動を即時に支援できるように最適化する必要があるのに対して、分析系の問い合わせでは、数秒や数分の遅延は許容範囲である
- データメッシュを実現するために区切られた文脈同士を連結させる最も基本的な方法は？
    - 共用サービス（共用サービスが提供する公開された言葉の一部は分析用に最適化することが可能）
- データメッシュを構築する技術方式として最も基本的なものは？
    - CQRS
- データメッシュでは分析系データを、ある領域ごとに分割する。ドメイン駆動設計の用語では、この領域のことをなんと言う？
    - 区切られた文脈
