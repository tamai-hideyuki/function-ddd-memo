## レイヤードアーキテクチャは技術的な関心ごとに基づいてソースコードを分解する。

- この方式は業務ロジックとデータアクセスの実装を結びつけるので、アクティブレコードベースのシステムに適している。

## ポートとアダプターは依存関係を逆転させる。（業務ロジックを中心に置き、業務ロジックを全ての基盤コンポーネントから独立させる）

- このやり方はドメインモデルで実装した業務ロジックに適している。

## CQRSは同じデータを複数のモデルで表現する。（イベント履歴式ドメインモデルに基づくシステムでは必須・他にも複数の永続化モデルを用いる必要があるシステムに利用できる技術方式）

